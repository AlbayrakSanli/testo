<%= form_with(model: furkan, local: true) do |form| %>
  <% if furkan.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(furkan.errors.count, "error") %> prohibited this furkan from being saved:</h2>

      <ul>
      <% furkan.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
